# trading_system/tests/fixtures/test_configs/model_test.yaml
models:
  test_mode: true
  walk_forward:
    window_size: 100
    step_size: 10
  
  xgboost:
    n_estimators: 100
    max_depth: 3
    learning_rate: 0.1
  
  lstm:
    sequence_length: 10
    units: [10, 5]
    epochs: 2
    batch_size: 32

training:
  test_size: 0.2
  validation_size: 0.1
  random_state: 42
