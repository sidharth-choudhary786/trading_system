# trading_system/tests/fixtures/test_configs/production_test.yaml
production:
  mode: "production"
  paper_trading: true
  broker: "mock"
  
risk:
  max_daily_loss: 0.02
  max_position_size: 0.1
  enable_circuit_breakers: true

monitoring:
  health_check_interval: 10
  alert_triggers: ["daily_loss_breach"]

execution:
  max_retries: 2
  retry_delay: 1
